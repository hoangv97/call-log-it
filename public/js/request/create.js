$(document).ready(() => {

    $('#deadline-picker').datetimepicker({
        autoclose: true,
        todayHighlight: true,
        fontAwesome: true,
        pickerPosition: 'bottom-left',
        container: '#deadline-picker'
    });

    $('#related-person').tagsinput({

    })

});